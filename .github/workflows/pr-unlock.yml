name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/github/workflows/shared-pr-unlock.yml@dev
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"
