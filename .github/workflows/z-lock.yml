name: lock-dispatch

on:
    push:

permissions:
    contents: write

jobs:
    lock:
        runs-on: ubuntu-latest
        steps:
            - uses: github/lock@v0.1.6
              id: lock
              with:
                  mode: "lock"
                  reason: "test"
                  environment: "d"

            - uses: github/lock@v0.1.6
              id: unlock
              with:
                  mode: "unlock"
                  environment: "d"
